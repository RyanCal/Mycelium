"""Docker exec wrappers placeholder."""

from __future__ import annotations


async def stream_exec(*_: object, **__: object) -> None:
    """Phase 2 streams stdout/stderr from Docker to the bus."""

    return None
