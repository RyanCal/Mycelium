# Proxmox CT Deployment

Deferred until Mycelium leaves the dev box. Mirror the web-apps tier pattern in
the homelab repo, document the service in `homelab/services/mycelium.md`, and
protect public access with Cloudflare Access before exposing the dashboard.
